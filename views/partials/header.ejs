<nav class="custom-nav">
  <a class="navbar-brand" href="#">
    <span class="logo-text">FeedHungry</span>
  </a>
  <ul class="nav-links nav-links-end">
    <% if (user) { %>
      <% if (user.isCustomer) { %>
        <li>
          <a href="#" class="nav-link open-login">Logout</a>
        </li>
      <% } else { %>
        <li>
          <a href="/logout" class="nav-link">Logout</a>
        </li>
      <% } %>
    <% } else { %>
      <li>
        <a href="#user-authentication" class="nav-link open-login">Login</a>
      </li>
    <% } %>
  </ul>
</nav>
